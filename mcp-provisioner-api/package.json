{
  "name": "mcp-provisioner-api",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@mp4marketing/dual-control": "^0.1.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "wrangler": "^3.90.0"
  }
}
